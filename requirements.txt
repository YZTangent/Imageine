# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9

# ML Core (Modern versions - 2025)
torch>=2.4.0
torchvision>=0.19.0
diffusers>=0.35.0
transformers>=4.40.0
accelerate>=0.30.0

# Image Processing
Pillow>=10.0.0
opencv-python>=4.9.0
numpy>=2.0.0

# Utilities
pyyaml>=6.0.1
requests>=2.31.0
aiofiles>=23.2.1

# Phase 2: Advanced Features
# These are now enabled for Phase 2 implementation
controlnet-aux>=0.0.7  # For pose/depth extraction
timm>=0.9.12  # Required by some models

# Optional but recommended for better performance
# xformers>=0.0.23  # For memory-efficient attention (requires compatible PyTorch/CUDA)
# Note: xformers installation may vary by system. See: https://github.com/facebookresearch/xformers

# Optional advanced detection (heavyweight, only if needed)
# groundingdino-py>=0.4.0  # Alternative GroundingDINO implementation
# segment-anything>=1.0  # For SAM-based segmentation
